const pi = 3.14;

export default pi;

export function area(r: number) {
  return pi * r ** 2;
}

export function area2(r: number) {
  return pi * r ** 2;
}

export function area3(r: number) {
  return pi * r ** 2;
}